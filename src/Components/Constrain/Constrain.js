import React from "react";
import { Container } from "react-bootstrap";

const Constrain = ({ con }) => {
    return (
        <ul>
            <li><span className="span-1">{con}</span></li>
        </ul>
    );
}

export default Constrain;